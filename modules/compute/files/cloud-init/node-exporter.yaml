#cloud-config
runcmd:
  - useradd --no-create-home --shell /bin/false node_exporter || true
  - curl -fL --retry 3 --connect-timeout 30 --max-time 300 -o /tmp/ne.tar.gz https://hb.ru-msk.vkcloud-storage.ru/lab56eoooi/artifacts/node_exporter-1.11.1.linux-amd64.tar.gz
  - tar xzf /tmp/ne.tar.gz -C /tmp/
  - cp /tmp/node_exporter-1.11.1.linux-amd64/node_exporter /usr/local/bin/
  - chown node_exporter:node_exporter /usr/local/bin/node_exporter
  - |
    cat > /etc/systemd/system/node_exporter.service <<'UNIT'
    [Unit]
    Description=Node Exporter
    After=network.target

    [Service]
    User=node_exporter
    Group=node_exporter
    Type=simple
    ExecStart=/usr/local/bin/node_exporter

    [Install]
    WantedBy=multi-user.target
    UNIT
  - systemctl daemon-reload
  - systemctl enable --now node_exporter
